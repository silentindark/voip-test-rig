# Minimal HTTPS server for the SIP-over-WebRTC web client.
ARG NGINX_TAG=alpine
FROM nginx:${NGINX_TAG}

COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY www/ /usr/share/nginx/html/

# TLS cert/key are mounted at /rig-certs (generated by certs/gen-certs.sh).
EXPOSE 443
